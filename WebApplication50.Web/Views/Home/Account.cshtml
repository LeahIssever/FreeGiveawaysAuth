@using WebApplication50.Data;
@model AccountViewModel

<div b-78gchgm0rj class="container">
    <main b-78gchgm0rj role="main" class="pb-3">

        <div class="row">
            <div class="col-md-6 offset-md-3">
                @if(Model.Listings != null)
                {
                    @foreach (Listing listing in Model.Listings)
                    {
                        <div class="bg-light rounded-3 p-5 mt-3">
                            <h5>@(listing.Name != null ? $"Listed by {@listing.Name}" : "")</h5>
                            <h5>Listed on @listing.DateCreated.ToLongDateString()</h5>
                            <h5>Phone Number: @listing.PhoneNumber</h5>
                            <h3>Details:</h3>
                            <p>@listing.Text</p>
                            <form method="post" action="/home/deletelisting">
                                <input type="hidden" name="id" value="@listing.Id" />
                                <button class="btn btn-danger">Delete</button>
                            </form>
                        </div>
                    }
                }
            </div>
        </div>
    </main>
</div>